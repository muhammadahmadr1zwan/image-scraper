# image-scraper environment variables
# Copy to ".env" in this directory. NEVER commit the real .env.

# Google Places API (New) - optional fallback when no override/Instagram source.
# Enable "Places API (New)" in Google Cloud Console for this key.
GOOGLE_PLACES_API_KEY=

# Optional: custom directory for downloaded images (default: ./output)
# OUTPUT_DIR=
